# Stage 0 uses these for doctor/config validation.
# Stage 1+ will use them for NCBI/GEO downloading and caching.

# Required for NCBI Entrez usage in later stages
NCBI_EMAIL="you@example.com"
NCBI_API_KEY=""

# Local dirs (used across stages)
GEO_CLEANSER_CACHE_DIR="./cache"
GEO_CLEANSER_OUTPUT_DIR="./outputs"

# Optional logging
GEO_CLEANSER_LOG_LEVEL="INFO"
